# Development environment variables
# This file is loaded by Vite when running `npm run dev`

# API URL for local development
# We use the '/api' prefix so Vite can proxy it to http://localhost:5000
VITE_API_URL=/api

# Feature Flags
VITE_SHOW_DEMO_CREDENTIALS=true
VITE_LOG_LEVEL=debug
